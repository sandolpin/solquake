<resources>
    <!-- UI全体はJetpack Composeで構築するため、XML側のテーマはウィンドウ属性のみ管理する -->
    <style name="Theme.WeatherQuake" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
